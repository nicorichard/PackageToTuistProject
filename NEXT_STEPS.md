We are in need of a system that allows us to add "ENABLE_TESTING_SEARCH_PATHS" : "YES" to the appropriate projects.

How should we handle packages that don't list a platform? I would assume we just use the minimum for all targets. Perhaps specifying a global minimum is necessary.