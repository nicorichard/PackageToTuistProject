Always add tests for new functionality, preferrably before the change is even made.

Before any task can be deemed complete run the tests using `swift test`. The task is not complete until ALL tests pass. AGAIN, don't stop iterating until tests have been run and are passing!
